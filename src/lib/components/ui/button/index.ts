import Root from "./button.svelte";
import {
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant
} from "./button.config";

export {
	Root,
	type ButtonProps as Props,
	//
	Root as Button,
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
};
